pipeline {
    triggers{
        cron('H/1 * * * *')
    }
    agent {
        docker {
            image 'openjdk:8-jdk-alpine'
            args '-p 8086:8080'
        }
    }
    
    stages {
        stage('1st step') {
             when { branch ‘dev' }
            steps {
                echo 'Hello, JDK'
                sh 'java -version'
            }
        }
    }
    
}